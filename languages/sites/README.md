# Per-locale site assets

Place optional, language-specific SEO files here to override the generated defaults:

- robots.txt
- sitemap.xml
- structured.json

If a file is missing, the backend generates a sensible default for that language.
